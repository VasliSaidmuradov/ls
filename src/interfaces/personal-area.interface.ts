export namespace IPersonalArea {
  export enum TabsName {
    USER_SETTINGS = 'user-settings',
    MEDICAL_CART = 'medical-card',
  }

  export enum PasswordStrength {
    RISKY = 'risky',
    QUESSABLE = 'guessable',
    WEEK = 'week',
    SAFE = 'safe',
    SECURE = 'secure'
  }
}
